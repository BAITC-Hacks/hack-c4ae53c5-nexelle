# The standalone demo uses persistent mock data by default.
VITE_API_MODE=mock

# Required only when VITE_API_MODE=real.
VITE_API_BASE_URL=http://localhost:8080

# Optional in real mode; defaults to E0001 for the hackathon demo.
VITE_DEMO_EMPLOYEE_ID=E0001
